# this is the "data/.gitignore" file
# Instruction: In the "data/.gitignore", place inside the following contents to track the empty directory (thus allowing CSV files to be written there without error), while ignoring the CSV files themselves

# ignore all files in this "data" directory:
*

# except this ".gitignore" file:
!.gitignore